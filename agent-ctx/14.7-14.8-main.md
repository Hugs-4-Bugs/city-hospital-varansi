# Phase 14.7 + 14.8: Performance + Mobile/Accessibility

## Task IDs: 14.7, 14.8
## Agent: main

## Summary
Implemented comprehensive performance optimizations and accessibility improvements for AcquisitionOS.

## Phase 14.7 — Performance Files Created

### 1. `/src/lib/performance/db-optimizer.ts`
- `analyzeQueryPerformance()` — Runs EXPLAIN QUERY PLAN, identifies missing indexes
- `getSlowQueries()` — Tracks queries >100ms using performance tracking
- `getQueryStats()` / `getQueryStatsSummary()` — Query timing statistics
- `optimizeDatabase()` — SQLite PRAGMA optimize, ANALYZE, WAL checkpoint
- `trackQuery()` — Wrapper for automatic query timing
- `buildCursorPagination()` — Cursor-based pagination (vs offset)
- `batchRead()` — Batch reads to prevent N+1 queries
- `selectFields()` — Select only needed fields
- `orderWhereConditions()` — Most selective WHERE first

### 2. `/src/lib/performance/cache-manager.ts`
- `get<T>()` / `set<T>()` / `invalidate(pattern)` / `invalidateAll()`
- `getOrSet<T>()` — Cache-aside pattern
- LRU eviction at 1000 entries
- Memory usage tracking
- Hit/miss ratio statistics
- Pre-configured TTLs: user(5min), subscription(2min), credits(1min), analytics(10min), competitor(30min)

### 3. `/src/lib/performance/api-cache.ts`
- `withApiCache(ttlMs)` — GET API route caching middleware
- `generateCacheKey()` — URL + query params + user ID cache key
- ETag support for conditional 304 responses
- Cache-Control headers (max-age, stale-while-revalidate)
- `CacheInvalidation` helpers for entity mutations
- `cacheControlHeaders()` — Header generator

### 4. `next.config.ts` — Enhanced
- `output: 'standalone'` for Docker
- `reactStrictMode: true`
- Image optimization: avif + webp formats, device sizes
- Font optimization enabled
- `removeConsole` in production (excluding error/warn)
- `optimizePackageImports` for lucide-react, date-fns, framer-motion
- Static asset caching headers (1yr immutable)
- API no-cache headers
- Security headers (X-Content-Type-Options, X-Frame-Options, etc.)
- Redirects for /dashboard, /app, /home
- Webpack split chunks with framework, lib, commons groups

### 5. Dashboard Layout — Verified
- Already has React.lazy() for all 12 tab components ✓
- Already has Suspense boundaries with SkeletonPage fallback ✓
- Already has skip-to-content link ✓

### 6. `/src/lib/performance/image-optimizer.ts`
- `getOptimizedImageConfig()` — Next.js Image configuration helper
- `generateResponsiveSizes()` — Pattern-based sizes (full-width, card, avatar, etc.)
- `generateBlurPlaceholder()` — SVG gradient blur data URLs
- `generateDominantColorPlaceholder()` — Single-color placeholder
- `getLazyLoadConfig()` — Connection-adaptive lazy loading
- `getImagePreload()` — Critical image preloading
- `generateResponsiveBackgroundCSS()` — image-set() CSS

### 7. `/src/lib/performance/realtime-optimizer.ts`
- `createDebouncer()` — 50ms default debounce
- `createBatcher()` — Window + max batch size batching
- `createThrottler()` — 5s default throttle with trailing call
- `measureLatency()` — WebSocket ping/pong latency
- `assessConnectionQuality()` — Latency → quality rating
- `createAdaptiveRateController()` — Auto-adjusts rate by connection
- `stripUnnecessaryFields()` — Payload compression
- Pre-configured: `createAnalyticsOptimizer()`, `createLeadUpdateOptimizer()`, `createPipelineOptimizer()`

## Phase 14.8 — Mobile + Accessibility Files Created

### 1. `/src/lib/accessibility/responsive-validator.ts`
- 9 breakpoints: 320, 375, 425, 768, 1024, 1280, 1440, 1920, 2560
- `mediaMin()` / `mediaMax()` / `mediaBetween()` — CSS media query helpers
- `useBreakpoint()` hook — Current breakpoint + isMobile/isTablet/isDesktop
- `getResponsiveValue<T>()` — Map-based responsive value selection
- `getResponsiveGrid()` — Auto-fill grid template
- `getViewportInfo()` — Width, height, DPR, touch, orientation

### 2. `/src/lib/accessibility/a11y-utils.ts`
- `announceToScreenReader(message, priority)` — ARIA live regions
- `trapFocus(containerElement)` — Focus trap for modals + `useFocusTrap` hook
- `getFocusableElements(container)` — Find all focusable children
- `handleKeyboardNav(event, handlers)` — Arrow key navigation + `useKeyboardNav` hook
- `visuallyHiddenStyles` — CSS for sr-only content
- `generateAriaLabel(element, context)` — Descriptive ARIA labels
- `checkContrast()` — WCAG AA (4.5:1) / AAA (7:1) contrast checker
- `createShortcutManager()` — Keyboard shortcut registration + management
- `useShortcuts()` — React hook for shortcut management
- `skipToContent()` — Skip-to-content focus management
- `prefersReducedMotion()` / `useReducedMotion()` — Motion preference detection

### 3. `/src/components/dashboard/accessibility-panel.tsx`
- Dev-only accessibility audit panel
- Checks: alt text, empty links/buttons, form labels, heading hierarchy, focus indicators, ARIA attributes, color contrast, keyboard nav
- Displays issues with severity + fix suggestions
- Category filtering
- Toggle with Ctrl+Shift+A keyboard shortcut
- Floating panel with badge showing issue count

### 4. `/src/components/dashboard/keyboard-shortcuts-help.tsx`
- Comprehensive shortcuts dialog with sections:
  - Global: ⌘K (command palette), ⌘N (new lead), Esc, ?, Ctrl+Shift+A
  - Navigation: 1-9 tabs, J/K for prev/next lead
  - Actions: E (enrich), S (score), G (generate outreach), N (new), D (delete), F (search)
- Accessible descriptions for screen readers
- Kbd component for styled shortcut keys

### 5. Skip-to-Content — Verified
- Already exists in dashboard-layout.tsx at line 365 ✓
- Links to `#main-content` with sr-only/focus-visible styling ✓

### 6. WCAG Color Contrast Check — globals.css
- Added comprehensive audit comments in both :root and .dark sections
- Light mode findings:
  - foreground/background: AAA ✓ (~19:1)
  - muted-foreground/background: AA borderline (~4.7:1) — suggested increase to oklch(0.45)
  - --warning on --bg: FAILS (~1.6:1) — decorative only
  - --danger on --bg: FAILS AA normal text (~2.9:1) — large text only
  - --accent-ds on --bg: FAILS (~2.4:1) — background only
- Dark mode findings:
  - foreground/background: AAA ✓ (~16:1)
  - --text-3 on --bg: FAILS (~2.6:1) — suggested increase to oklch(0.5)

## Lint Status
All new files pass ESLint. Remaining errors are pre-existing in server.js/process-manager.js/mini-services.
