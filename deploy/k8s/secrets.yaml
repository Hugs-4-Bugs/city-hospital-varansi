# ═══════════════════════════════════════════════════════════════════
# AcquisitionOS — Kubernetes Secrets (TEMPLATE)
# Phase 14.4: Deployment
# ⚠️  DO NOT commit with real values — replace with your actual secrets
#     or use sealed-secrets / external-secrets-operator in production
# ═══════════════════════════════════════════════════════════════════
apiVersion: v1
kind: Secret
metadata:
  name: acquisitionos-secrets
  namespace: acquisitionos
type: Opaque
stringData:
  POSTGRES_PASSWORD: "REPLACE_ME_WITH_STRONG_PASSWORD"
  REDIS_PASSWORD: "REPLACE_ME_WITH_STRONG_PASSWORD"
  SECRET_KEY: "REPLACE_ME_WITH_RANDOM_64CHAR_HEX"
  JWT_SECRET: "REPLACE_ME_WITH_RANDOM_64CHAR_HEX"
  GRAFANA_ADMIN_PASSWORD: "REPLACE_ME_WITH_STRONG_PASSWORD"
---
apiVersion: v1
kind: Secret
metadata:
  name: acquisitionos-tls
  namespace: acquisitionos
type: kubernetes.io/tls
data:
  tls.crt: "REPLACE_WITH_BASE64_ENCODED_CERT"
  tls.key: "REPLACE_WITH_BASE64_ENCODED_KEY"
